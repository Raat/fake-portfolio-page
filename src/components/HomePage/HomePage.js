import React from 'react';
import { CarouselContainer, CarouselItem } from '../Carousel';

const HomePage = () => (
  <div>
    <CarouselContainer>
      <CarouselItem />
    </CarouselContainer>
  </div>
);

export default HomePage;
